# Frontend - Система управления задачами

Современный веб-интерфейс для системы управления задачами SkillTracker.

## Возможности

### Для всех пользователей
- 🔐 Аутентификация (вход/регистрация)
- 📋 Просмотр задач
- 💬 Комментирование задач
- 📎 Загрузка файлов к задачам
- 📊 Отслеживание прогресса

### Для сотрудников (Employee)
- Просмотр назначенных задач
- Обновление прогресса выполнения
- Изменение статуса задачи
- Добавление комментариев
- Загрузка файлов

### Для менеджеров (Manager)
- Все функции сотрудника
- Создание новых задач
- Просмотр всех задач всех сотрудников
- Управление пользователями
- Удаление задач
- Редактирование задач

## Технологии

- **React 19** - современная библиотека для создания UI
- **Vite** - быстрый сборщик и dev-сервер
- **Fetch API** - для работы с бэкендом
- **CSS3** - современные стили с градиентами

## Установка и запуск

### Установка зависимостей

```powershell
npm install
```

### Запуск в режиме разработки

```powershell
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### Сборка для продакшена

```powershell
npm run build
```

Собранные файлы будут в папке `dist/`.

## Структура проекта

```
src/
├── api.js              # API клиент для работы с бэкендом
├── App.jsx             # Главный компонент приложения
├── App.css             # Основные стили
├── main.jsx            # Точка входа
├── index.css           # Базовые стили
└── components/         # Компоненты
    ├── Login.jsx           # Страница входа
    ├── Register.jsx        # Страница регистрации
    ├── Dashboard.jsx       # Главная панель
    ├── TaskList.jsx        # Список задач
    ├── TaskDetail.jsx      # Детали задачи
    ├── CreateTask.jsx      # Создание задачи
    ├── UserList.jsx        # Список пользователей
    └── CreateUser.jsx      # Создание пользователя
```

## API Endpoint

Приложение использует API по адресу: `https://apiskilltracker.tw1.ru/api`

Для изменения адреса API отредактируйте константу `API_BASE_URL` в файле `src/api.js`.

## Использование

### Первый запуск

1. Зарегистрируйте первого пользователя с ролью "Менеджер"
2. Войдите в систему
3. Создайте сотрудников через вкладку "Пользователи"
4. Создайте задачи для сотрудников

### Роли пользователей

**Менеджер (manager)**
- Полный доступ к системе
- Может создавать задачи и пользователей
- Видит все задачи всех сотрудников

**Сотрудник (employee)**
- Видит только свои задачи
- Может обновлять прогресс и статус
- Может комментировать и загружать файлы

## Основные функции

### Задачи

- **Создание** (только менеджеры): назначение задачи сотруднику с описанием и сроком
- **Просмотр**: детальная информация о задаче
- **Обновление прогресса**: ползунок от 0 до 100%
- **Изменение статуса**: Ожидает / В работе / Завершена
- **Комментарии**: обсуждение задачи в реальном времени
- **Вложения**: загрузка файлов к задаче

### Пользователи

- **Создание**: добавление новых сотрудников (только менеджеры)
- **Просмотр**: список всех пользователей с ролями

## Дизайн

Интерфейс выполнен в современном стиле с:
- Градиентными фонами
- Плавными анимациями
- Адаптивным дизайном
- Интуитивной навигацией
- Красивыми карточками и модальными окнами

## Лицензия

MIT
